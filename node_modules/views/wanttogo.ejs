<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Want-To-Go List</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
  
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      background-attachment: fixed;
      min-height: 100vh;
      font-family: 'Poppins', sans-serif;
      padding-bottom: 50px;
    }

    .navbar {
      background: rgba(0, 0, 0, 0.8) !important;
      backdrop-filter: blur(10px);
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    }

    .navbar-brand {
      font-size: 1.5rem;
      font-weight: 600;
      color: #fff !important;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
    }

    .container {
      margin-top: 40px;
    }

    h1 {
      color: white;
      text-align: center;
      font-weight: 600;
      font-size: 2.5rem;
      text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.3);
      margin-bottom: 40px;
      animation: fadeIn 0.6s ease-in;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .empty-state {
      background: rgba(255, 255, 255, 0.95);
      padding: 60px 40px;
      border-radius: 20px;
      text-align: center;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
      animation: fadeIn 0.8s ease-in;
    }

    .empty-state img {
      width: 150px;
      opacity: 0.6;
      margin-bottom: 20px;
    }

    .empty-state h3 {
      color: #667eea;
      font-weight: 600;
      margin-bottom: 15px;
    }

    .empty-state p {
      color: #666;
      font-size: 1.1rem;
      margin-bottom: 25px;
    }

    .btn-explore {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 12px 35px;
      border: none;
      border-radius: 25px;
      font-weight: 600;
      text-decoration: none;
      display: inline-block;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
    }

    .btn-explore:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);
      color: white;
      text-decoration: none;
    }

    .destinations-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 25px;
      animation: fadeIn 0.8s ease-in;
    }

    .destination-card {
      background: rgba(255, 255, 255, 0.95);
      border-radius: 15px;
      padding: 25px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .destination-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 5px;
      background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
    }

    .destination-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.25);
    }

    .destination-icon {
      width: 60px;
      height: 60px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 15px;
      font-size: 28px;
    }

    .destination-name {
      font-size: 1.5rem;
      font-weight: 600;
      color: #333;
      margin-bottom: 15px;
    }

    .destination-actions {
      display: flex;
      gap: 10px;
      margin-top: 20px;
    }

    .btn-view {
      flex: 1;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 8px;
      font-weight: 500;
      cursor: pointer;
      transition: transform 0.2s ease;
    }

    .btn-view:hover {
      transform: scale(1.05);
    }

    .btn-remove {
      background: #dc3545;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 8px;
      font-weight: 500;
      cursor: pointer;
      transition: transform 0.2s ease;
    }

    .btn-remove:hover {
      transform: scale(1.05);
      background: #c82333;
    }

    .list-count {
      text-align: center;
      color: white;
      font-size: 1.2rem;
      margin-bottom: 30px;
      font-weight: 400;
    }

    .list-count strong {
      font-weight: 600;
      font-size: 1.4rem;
    }

    .back-home {
      text-align: center;
      margin-top: 40px;
    }

    .btn-back {
      background: rgba(255, 255, 255, 0.2);
      color: white;
      padding: 12px 30px;
      border: 2px solid white;
      border-radius: 25px;
      font-weight: 600;
      text-decoration: none;
      display: inline-block;
      transition: all 0.3s ease;
    }

    .btn-back:hover {
      background: white;
      color: #667eea;
      text-decoration: none;
      transform: translateY(-2px);
    }
  </style>
</head>

<body>

<nav class="navbar navbar-expand-lg navbar-dark">
  <div class="container-fluid">
    <a class="navbar-brand">
      ✈️ My Want-To-Go List
    </a>
  </div>
</nav>

<div class="container">
  <h1>Your Travel Dreams</h1>

  <% if (list.length === 0) { %>
    <div class="empty-state">
      <div style="font-size: 80px; margin-bottom: 20px;">🌍</div>
      <h3>Your list is empty!</h3>
      <p>Start adding destinations you want to visit and plan your next adventure.</p>
      <a href="/home" class="btn-explore">Explore Destinations</a>
    </div>
  <% } else { %>
    <div class="list-count">
      You have <strong><%= list.length %></strong> destination<%= list.length !== 1 ? 's' : '' %> on your list
    </div>

    <div class="destinations-grid">
      <% list.forEach((destination, index) => { 
        // Map destinations to icons and URLs
        const destinationInfo = {
          'Paris': { icon: '🗼', url: '/paris', category: 'City' },
          'Rome': { icon: '🏛️', url: '/rome', category: 'City' },
          'Bali': { icon: '🏝️', url: '/bali', category: 'Island' },
          'Santorini': { icon: '🌅', url: '/santorini', category: 'Island' },
          'Annapurna': { icon: '⛰️', url: '/annapurna', category: 'Hiking' },
          'Inca Trail': { icon: '🥾', url: '/inca', category: 'Hiking' }
        };
        
        const info = destinationInfo[destination] || { icon: '📍', url: '#', category: 'Destination' };
      %>
        <div class="destination-card">
          <div class="destination-icon"><%= info.icon %></div>
          <div class="destination-name"><%= destination %></div>
          <div style="color: #666; font-size: 0.9rem; margin-bottom: 10px;">
            <%= info.category %>
          </div>
          <div class="destination-actions">
            <button class="btn-view" onclick="location.href='<%= info.url %>'">
              View Details
            </button>
            <form action="/remove-from-list" method="POST" style="margin: 0;">
              <input type="hidden" name="destinationName" value="<%= destination %>">
              <button type="submit" class="btn-remove" onclick="return confirm('Remove <%= destination %> from your list?')">
                Remove
              </button>
            </form>
          </div>
        </div>
      <% }) %>
    </div>

    <div class="back-home">
      <a href="/home" class="btn-back">← Back to Home</a>
    </div>
  <% } %>
</div>

</body>
</html>