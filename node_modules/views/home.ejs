<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Destinations</title>

    <link rel="stylesheet"
          href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
          crossorigin="anonymous">

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;600&display=swap" rel="stylesheet">

    <style>
        body {
            margin: 0;
            background: #111 !important;
            min-height: 100vh;
            color: white;
            font-family: 'Poppins', sans-serif;
        }

        /* Add logout button INSIDE the header, not absolute */
        .logout-header-btn {
            margin-left: auto;
            margin-right: 10px;
            padding: 6px 16px;
            border-radius: 8px;
        }

        /* Main content */
        .main-content {
            margin-top: 60px;
            text-align: center;
        }

        .welcome-title {
            font-size: 3.2rem;
            font-weight: 600;
            color: #cfd8ff;
            animation: fadeIn 1.3s ease-in-out;
        }

        @keyframes fadeIn {
            0% { opacity: 0; transform: translateY(-10px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        /* Category section */
        .category-row {
            margin-top: 50px;
            display: flex;
            justify-content: center;
            gap: 60px;
            flex-wrap: wrap;
        }

        .category-card {
            background: #1c1c1c;
            padding: 25px 20px;
            border-radius: 25px; /* bubble shape */
            width: 230px;
            text-align: center;
            transition: 0.25s ease-in-out;
            box-shadow: 0 0 12px rgba(255,255,255,0.05);
        }

        .category-card:hover {
            transform: scale(1.08);
            box-shadow: 0 0 20px rgba(255,255,255,0.18);
        }

        .category-card img {
            width: 150px;
            height: 150px;
            object-fit: contain;
            margin-bottom: 10px;
        }

        .want-to-go-btn {
            margin-top: 50px;
            min-width: 260px;
            padding: 10px 20px;
            font-size: 1.1rem;
            border-radius: 10px;
        }
    </style>
</head>

<body>

    <!-- HEADER WITH SEARCH BAR + LOGOUT BUTTON -->
    <div style="display:flex; align-items:center; background:#222; padding:12px 20px;">
        <%- include("partials/header") %>

        <!-- Logout button safely placed on right side -->
        <button class="btn btn-danger logout-header-btn" onclick="location.href='/logout'">
            Logout
        </button>
    </div>

    <div class="main-content">

        <h1 class="welcome-title">Welcome</h1>

        <div class="category-row">

            <div class="category-card">
                <img src="/hiking.png" alt="Hiking">
                <div class="mt-2 mb-2">Hiking</div>
                <button class="btn btn-secondary" onclick="location.href='/hiking'">View</button>
            </div>

            <div class="category-card">
                <img src="/cities.png" alt="Cities">
                <div class="mt-2 mb-2">Cities</div>
                <button class="btn btn-secondary" onclick="location.href='/cities'">View</button>
            </div>

            <div class="category-card">
                <img src="/islands.png" alt="Islands">
                <div class="mt-2 mb-2">Islands</div>
                <button class="btn btn-secondary" onclick="location.href='/islands'">View</button>
            </div>

        </div>

        <button class="btn btn-secondary want-to-go-btn" onclick="location.href='/wanttogo'">
            Want-to-Go List
        </button>
    </div>

</body>
</html>